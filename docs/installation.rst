============
Installation
============

At the command line::

    $ easy_install vacancy-telegrambot

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv vacancy-telegrambot
    $ pip install vacancy-telegrambot
